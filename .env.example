# Schyrim AI Provider Configuration
# Copy this file to .env (which is not tracked by git) and add your API keys
# Leave keys blank for disabled providers — game will use the next available provider in priority order

# === ANTHROPIC (Claude) ===
# Get API key from: https://console.anthropic.com/
# Free tier available: https://www.anthropic.com/pricing
# Recommended for best quality. Supports:
#   - claude-3-5-sonnet-20241022 (high quality, slower)
#   - claude-3-5-haiku-20241022 (fast, great for game narration) [DEFAULT]
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-haiku-20241022

# === OPENROUTER (Multi-provider) ===
# Get API key from: https://openrouter.ai/
# Supports hundreds of models from Meta, Mistral, Llama, etc.
# Free credits available for testing
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct:free

# === GROQ (Fast inference) ===
# Get API key from: https://console.groq.com/
# Free tier available: https://console.groq.com/keys
# Fastest inference, great for real-time responses
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# === PROVIDER PRIORITY ===
# If multiple API keys are set, Schyrim will use this priority:
#   1. ANTHROPIC_API_KEY (Claude)
#   2. OPENROUTER_API_KEY (hundreds of models)
#   3. GROQ_API_KEY (fast)
#   4. Mock provider (offline, no key needed)
#
# To use only one provider, set the others to empty strings (or don't set them)
# Example: Use only Anthropic
#   ANTHROPIC_API_KEY=sk-ant-v7-...
#   OPENROUTER_API_KEY=
#   GROQ_API_KEY=
